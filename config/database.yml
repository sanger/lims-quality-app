development:
  adapter: sqlite
  database: dev.db

test:
  adapter: sqlite
  database: test.db

development_mysql:
  adapter: mysql2
  encoding: utf8
  charset: utf8
  database: quality_development
  username: root
  password: root
  socket: /tmp/mysql.sock

development_mysql_jdbc:
  adapter: jdbc
  url: "mysql://localhost/quality_development?user=root&password=root"
  charset: utf8
  encoding: utf8

